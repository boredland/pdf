# OCR app — remaining work

## Done (commits)
- [x] Step 0   Scaffold                              75925b7
- [x] Step 1   Storage + artifacts + SW              3b5231f
- [x] Step 2   PDF ingest + render                   cf25c87
- [x] Step 3   OpenCV preprocess                     314368d
- [x] Step 4   Text-region detection                 9dfb8ee
- [x] Step 5   Tesseract OCR                         480db94
- [x] Step 6   Gemini hosted provider                c7a90e7
- [x] Step 7   MRC split (JBIG2 + JP2)               15dbb2d
- [x] Step 7.5 Per-page detail pane                  a1a967c
- [x] Step 8   PDF builder + text layer              8d3a845
- [x] Step 9   Orchestrator + resume + confirm       2ae9b5f
- [x] Manual   cardinal.pdf example                  f12ffbd
- [x] Manual   mask PNG optimization                 dcba3f4
- [x] Manual   Auto image rotation (EXIF+content)    bef25c2
- [x] Manual   Document 180° rotation limitation     238966a
- [x] E2E speedup (~7m → 2m20s) + mask-worker fix    7fc8988
- [x] Image upload fix + step11 spec                 0a4232c

## Step 10 — Polish (remaining)
- [ ] Keyboard navigation across full UI (tab order, focus rings, shortcuts)
- [ ] axe accessibility scan, zero WCAG AA violations
- [ ] Visual regression screenshots: empty / ingest / mid-job / done / error
- [x] Cold-load → example job → reload offline → reopen → re-run page →
      export (step13 e2e spec)                              (shipped)
- [x] Asset-cache pane polish: per-bucket rows with entry counts,
      per-bucket purge, online/offline pill indicator.        (shipped)
- [ ] Error-state surfaces across panels (provider failure, OPFS full, etc.)
- [x] Display original vs. built PDF size (and the delta) on the project
      header so users see the MRC compression payoff (or penalty).
- [ ] Let users drop pages from the project (e.g. blank cover pages,
      back-matter they don't want OCR'd). Needs: per-page "remove"
      control, invalidation of downstream artifacts for the removed
      index, and a re-index pass so page numbers stay contiguous.
- [x] Bump thumbnail resolution 160 → 320 across render/preprocess/mrc
      (HiDPI crispness).                                    (shipped)

## Rotation detection without EXIF
- [x] Tesseract OSD in preprocess: all four cardinal rotations
      (0/90/180/270) via OpenCV cv.rotate + canvas-based dim swap in
      MRC. Validated end-to-end on cardinal.pdf's 4-page fixture.
- [ ] Raise OSD confidence threshold (currently 2.0) after measuring on a
      real scan corpus — lower FP rate on ambiguous pages.
- [x] Verified osd/eng traineddata already match the SW runtime
      CacheFirst rule (`/\.traineddata(\.gz)?$/i`). Lazy-cached on
      first fetch, offline thereafter — matches the delivery plan.
- [ ] Provide a manual "rotate page" control in the detail pane (and
      probably as a quick action on the page card too) as an escape
      hatch when OSD gets it wrong. Needs: per-page rotation override
      stored on the Page row, participates in preprocess hash so a
      change invalidates downstream stages cleanly. Four-way toggle:
      0° / 90° CW / 180° / 90° CCW.

## Automatic language detection
- [ ] Detect the page language(s) before OCR runs so users don't have to
      pick. Two approaches worth prototyping:
        a) Run Tesseract OSD first (already loaded for rotation) — it
           returns `script` + `script_confidence`. Map Latin/Cyrillic/
           Arabic/CJK/etc. to a default language code, let users confirm.
        b) Kreuzberg-style: run a tiny LSTM on a sampled strip of each
           page, vote across pages, pick the traineddata. Offline-safe
           once the model ships (~1 MB).
- [ ] Once detection is in, auto-download the selected traineddata via
      the existing SW cache path and add a "Detected: <lang>" pill in
      the language selector.

## Output compression
- [x] Builder: embed mask + background separately (was: lossless PNG of
      the composed image, ~18× source size) → ~7-8× after mask+bg split
      and 0.75-scale mask. Further drops blocked on JBIG2.  (shipped)
- [ ] Real JBIG2 encoder for the mask layer — **highest-impact remaining
      compression item**. Our "optimized mask PNG" is 8-bit RGBA with
      all pixels clamped to 0/255 and flate-compressed by the Canvas
      PNG encoder; a real 1-bit-per-pixel encoding via jbig2enc gets
      another 5-10× reduction on text-heavy pages. jbig2enc-wasm exists
      but builds are flaky — vendor our own if needed.
- [ ] Real JPEG 2000 encoder for the background layer (current output is
      JPEG via Canvas). openjpeg-wasm is the candidate.
- [ ] Use PDF native /ImageMask for the mask layer instead of an alpha
      PNG. Requires dropping from pdf-lib's high-level API to PDFDict
      internals, but it's the canonical MRC representation and lets
      viewers avoid compositing cost.
- [ ] Skip MRC split entirely for pure-B&W pages — emit a single bitonal
      image. Saves the background-layer bytes.
- [ ] Investigate zstd PDF stream filtering (PDF 2.0 feature) for
      non-image streams — mupdf support TBD.

## Deferred follow-ups (from memory)
- [ ] pino-style structured browser logging (replace ad-hoc console.warn)
- [ ] Multi-provider readiness (Mistral OCR, Doubleword, Gemma) behind registry

## Known-broken / untested corners surfaced during the speedup pass
- [ ] Tests that still run real Tesseract (step5, step6, step7-6, step8
      invisible-text) pull eng.traineddata from jsDelivr on first run — brittle
      in CI without a warm SW cache. Consider pre-seeding via a global setup.
- [ ] The "mock" OCR provider is registered in prod bundles too. Harmless
      but noisy; gate behind a build flag or the testing harness.
- [ ] No e2e coverage for: dropping a real PDF via the drop zone (we go
      through the harness), abort during build, OPFS quota exhaustion.
- [ ] `optimize-mask` threshold is hard-coded at 128 — no exposure in
      settings, no visual regression. Fine today, flag if it starts
      clipping antialiased masks.

## Stretch (post-v1)
- [ ] CRAFT ONNX text detector (swap MSER when better IoU proven)
- [ ] Optional CORS proxy worker for blocked providers
- [ ] Dewarping (book-spine curvature)
- [ ] Batch import (ZIP / multi-PDF)
- [ ] Export hOCR / ALTO XML
